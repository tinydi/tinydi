/**
 * metadata
 * @export
 * @interface Metadata
 * @template T
 */
export interface Metadata {
  /**
   * decorator name for the metadata
   * @type {string[]}
   * @memberOf Metadata
   */
  decorator?: string[];
}
